# Add local libdeps include dirs for CLion code insight
file(GLOB_RECURSE PIO_LIBS "${CMAKE_CURRENT_LIST_DIR}/.pio/libdeps/*/*/src")
foreach(LIB ${PIO_LIBS})
    get_filename_component(LIB_DIR ${LIB} DIRECTORY)
    include_directories(${LIB_DIR})
endforeach()

# Add specific libraries without src directory structure
include_directories("${CMAKE_CURRENT_LIST_DIR}/.pio/libdeps/controllino_mega/OneWire")
include_directories("${CMAKE_CURRENT_LIST_DIR}/.pio/libdeps/controllino_mega/DallasTemperature")